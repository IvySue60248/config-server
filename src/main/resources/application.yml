spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IvySue60248/config-repo-demo.git
server:
  port: 1201

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/

